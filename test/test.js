var assert = require('assert');
var getCssColors = require('..');

describe('get-css-colors', function() {

  it('should do something awesome', function() {
    assert.equal(getCssColors(), true);
  });
});
